# Artefacts de présentation (hors dossier projet — ne pas versionner)
presentation-scribe.html
presentation-*.html

# Export handoff du design system (généré, source = projet Claude Design ;
# logos/icônes déjà dans public/, design system dans DESIGN.md) — local only
Branding/

# Secrets / env (jamais commiter les clés Supabase, Anthropic, OpenAI, Stripe)
.env
.env.*
!.env.example

# Dépendances / build
node_modules/
/.pnp
.pnp.*
.next/
/out/
dist/
build/
/coverage

# Playwright (sorties de tests — garder tests/ et playwright.config.ts)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Artefacts du pilotage navigateur via le MCP Playwright (snapshots, console, captures)
/.playwright-mcp/

# Next.js / TypeScript / Vercel
next-env.d.ts
*.tsbuildinfo
.vercel
*.pem

# Supabase (CLI : garder migrations/ et config.toml, ignorer le reste local)
.branches
.temp
supabase/.temp/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Cache de recherche web (firecrawl) — local, ne jamais versionner
.firecrawl/
.firecrawl_key

# Scripts / fichiers parasites à la racine (ne pas versionner)
__pycache__/
*.pyc
mission_*.md
mission_*.py
enrich_emails.py
fix_phones.py
plan_cleanup.md
cc_cleanup_plan.md
cc_cleanup_prompt.txt
skills-lock.json

# Système
.DS_Store
